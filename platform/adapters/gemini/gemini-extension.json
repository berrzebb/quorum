{
  "name": "quorum",
  "version": "0.5.0",
  "description": "Cross-model audit gate — parliament protocol, 5 enforcement gates, 26 MCP tools, 3030 tests.",
  "mcpServers": {
    "quorum": {
      "command": "node",
      "args": [
        "${extensionPath}${/}..${/}..${/}core${/}tools${/}mcp-server.mjs"
      ],
      "cwd": "${extensionPath}${/}..${/}..${/}.."
    }
  }
}
