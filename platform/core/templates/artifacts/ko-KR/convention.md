# {project_name} 코딩 규칙

> quorum planner가 생성. `blueprint_lint` 도구로 검증.

**버전**: 1.0
**생성일**: {date}

---

## 1. 네이밍 규칙

### 파일 & 폴더

| 대상 | 규칙 | 예시 |
|------|------|------|
| {대상} | {규칙} | {예시} |

### 코드 식별자

| 대상 | 규칙 | 예시 |
|------|------|------|
| {대상} | {규칙} | {예시} |

> 이 테이블은 `quorum tool blueprint_lint`가 파싱합니다. `| 대상 | 규칙 | 예시 |` 헤더 형식을 유지하세요.

---

## 2. 파일 구조

```
{root}/
├── {디렉토리}/    # {용도}
└── {디렉토리}/    # {용도}
```

### 레이어 규칙

| 레이어 | 위치 | 책임 | 참조 가능 |
|--------|------|------|----------|
| {레이어} | {경로} | {책임} | {허용 import} |

---

## 3. Import 순서

```
// 1. 표준 라이브러리 / 내장 모듈
// 2. 외부 패키지 (third-party)
// 3. 내부 절대 경로 import
// 4. 상대 경로 import
// 5. 타입 전용 import
// 6. 사이드 이펙트 import (스타일, 폴리필)
```

---

## 4. 에러 처리 패턴

| 레이어 | 전략 | 예시 |
|--------|------|------|
| 진입점 | 전체 catch + 구조화 로그 | `try/catch → logger.error()` |
| 비즈니스 로직 | 타입 에러 / Result 패턴 | `throw new AppError(code)` |
| 외부 I/O | 재시도 + 서킷 브레이커 | `withRetry(fetch, { max: 3 })` |

---

## 5. 테스트 규칙

| 항목 | 규칙 |
|------|------|
| 테스트 파일 위치 | {패턴} |
| 테스트 파일 네이밍 | {패턴} |
| 테스트 러너 | {도구} |
| 최소 커버리지 | Statement ≥ 85%, Branch ≥ 75% |
| 픽스처 위치 | {경로} |

---

## 6. 커밋 & 브랜치 규칙

### 커밋 메시지 형식

```
type(scope): 설명

Types: feat, fix, docs, style, refactor, test, chore, perf
```

### 브랜치 네이밍

```
{type}/{ticket-id}-{간단-설명}
```

---

## 검증 체크리스트

- [ ] 모든 식별자 유형에 대한 네이밍 규칙 정의됨
- [ ] 레이어 규칙과 함께 파일 구조 문서화됨
- [ ] Import 순서 지정됨
- [ ] 레이어별 에러 처리 패턴 정의됨
- [ ] 테스트 규칙 수립됨
- [ ] 커밋/브랜치 규칙 문서화됨
