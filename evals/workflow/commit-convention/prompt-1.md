# Commit Convention Eval Scenario

Write a commit message for the following staged changes:

## Context

- Staged files:
  - `src/auth/middleware.ts` — added input validation
  - `src/auth/login.ts` — refactored error handling
  - `tests/auth.test.ts` — added 5 new test cases
- Project CLAUDE.md says: "Use English for commit subjects"

## Task

Execute `/quorum:commit-convention` to determine the right commit(s).
