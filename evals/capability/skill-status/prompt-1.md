# Skill Inventory Health Check

Run a full skill inventory scan across all layers. Show the compatibility matrix, detect any issues (missing wrappers, description mismatches, trigger conflicts), and report eval coverage.
