# {project_name} Coding Conventions

> Generated by quorum planner. Enforced by `blueprint_lint` tool.

**Version**: 1.0
**Created**: {date}

---

## 1. Naming Conventions

### Files & Folders

| Target | Rule | Example |
|--------|------|---------|
| {target} | {convention} | {example} |

### Code Identifiers

| Target | Rule | Example |
|--------|------|---------|
| {target} | {convention} | {example} |

> These tables are parsed by `quorum tool blueprint_lint`. Keep the `| Target | Rule | Example |` header format.

---

## 2. File Organization

```
{root}/
├── {directory}/    # {purpose}
└── {directory}/    # {purpose}
```

### Layer Rules

| Layer | Location | Responsibility | Can Import |
|-------|----------|----------------|------------|
| {layer} | {path} | {responsibility} | {allowed imports} |

---

## 3. Import Order

```
// 1. Standard library / built-in modules
// 2. External packages (third-party)
// 3. Internal absolute imports
// 4. Relative imports
// 5. Type-only imports
// 6. Side-effect imports (styles, polyfills)
```

---

## 4. Error Handling Patterns

| Layer | Strategy | Example |
|-------|----------|---------|
| Entry point | Catch-all + structured log | `try/catch → logger.error()` |
| Business logic | Typed errors / Result pattern | `throw new AppError(code)` |
| External I/O | Retry + circuit breaker | `withRetry(fetch, { max: 3 })` |

---

## 5. Testing Conventions

| Aspect | Convention |
|--------|-----------|
| Test file location | {pattern} |
| Test file naming | {pattern} |
| Test runner | {tool} |
| Minimum coverage | Statement ≥ 85%, Branch ≥ 75% |
| Fixture location | {path} |

---

## 6. Commit & Branch Conventions

### Commit Message Format

```
type(scope): description

Types: feat, fix, docs, style, refactor, test, chore, perf
```

### Branch Naming

```
{type}/{ticket-id}-{short-description}
```

---

## Validation Checklist

- [ ] Naming conventions defined for all identifier types
- [ ] File organization documented with layer rules
- [ ] Import order specified
- [ ] Error handling patterns per layer
- [ ] Testing conventions established
- [ ] Commit/branch conventions documented
