{
  "name": "quorum",
  "version": "0.4.4",
  "description": "Cross-model audit gate — parliament protocol, 5 enforcement gates, 21 MCP tools, 1018 tests.",
  "mcpServers": {
    "quorum": {
      "command": "node",
      "args": [
        "${extensionPath}${/}..${/}..${/}core${/}tools${/}mcp-server.mjs"
      ],
      "cwd": "${extensionPath}${/}..${/}.."
    }
  }
}
