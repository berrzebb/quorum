{
  "name": "quorum",
  "version": "0.4.2",
  "description": "Cross-model audit gate with autonomous quality loop — AST hybrid scanning, fitness gate, blast radius analysis, structured orchestration.",
  "mcpServers": {
    "quorum": {
      "command": "node",
      "args": ["${extensionPath}${/}..${/}..${/}core${/}tools${/}mcp-server.mjs"],
      "cwd": "${extensionPath}${/}..${/}.."
    }
  }
}
