{
  "name": "quorum",
  "version": "0.4.2",
  "description": "Cross-model audit gate — 3-adapter, HookRunner engine, multi-model NDJSON, 19 MCP tools, 921 tests.",
  "mcpServers": {
    "quorum": {
      "command": "node",
      "args": ["${extensionPath}${/}..${/}..${/}core${/}tools${/}mcp-server.mjs"],
      "cwd": "${extensionPath}${/}..${/}.."
    }
  }
}
